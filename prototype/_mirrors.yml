# Зеркала для dead man's switch (документ 10 §10.5).
# Боевой вариант: IPFS-адреса и HTTP-зеркала в разных юрисдикциях.
# Прототип: каталоги-приёмники (контракт публикации тот же).
mirrors:
  - ./mirrors/local-alpha
  - ./mirrors/local-beta
