{
  "@context": "https://www.w3.org/ns/activitystreams",
  "type": "Article",
  "id": "https://zh-lib.local/federation/kn-2026-0201",
  "attributedTo": "https://zh-lib.local/federation/actors/pavel_d",
  "name": "Типовая причина переобучения малой модели",
  "summary": "статус: aktualno · версия 3 · проверено 2026-07-02",
  "updated": "2026-07-02",
  "content": "<p>Эта карточка отвечает на вопрос: <strong>почему маленькая модель быстро запоминает обучающую выборку и перестаёт обобщать — и что проверить в первую очередь.</strong> (Уровень источника: C — опыт сообщества, дистиллировано из чата.)</p>\n<h2>Симптомы</h2>\n<ul><li>Loss на обучении падает, на валидации растёт уже после ~2 эпох.</li><li>Модель уверенно отвечает на примеры из train и молчит на новых.</li></ul>\n<h2>Типовая причина</h2>\n<p>В 8 из 10 случаев, разобранных в чате, причина одна: <strong>обучающая выборка слишком мала и не перемешана</strong> — модель видит одни и те же примеры в одном порядке и запоминает их как таблицу.</p>\n<h2>Что проверить (по порядку)</h2>\n<ol><li>Перемешан ли датасет между эпохами (shuffle).</li><li>Доля дубликатов и near-duplicates в train (быстрый тест — по хешам нормализованного текста).</li><li>Соотношение «параметры модели / число примеров»: примеров меньше, чем параметров, — жди запоминания.</li><li>Early stopping по валидационному loss включён ли вообще.</li></ol>\n<h2>Когда причина другая</h2>\n<p>Если пункты 1–4 чисты, типовые варианты: слишком высокий learning rate (проверить кривую на первых 100 шагах) или утечка train в валидацию.</p>",
  "source": {
    "content": "Эта карточка отвечает на вопрос: **почему маленькая модель быстро запоминает обучающую выборку и перестаёт обобщать — и что проверить в первую очередь.** (Уровень источника: C — опыт сообщества, дистиллировано из чата.)\n\n## Симптомы\n\n- Loss на обучении падает, на валидации растёт уже после ~2 эпох.\n- Модель уверенно отвечает на примеры из train и молчит на новых.\n\n## Типовая причина\n\nВ 8 из 10 случаев, разобранных в чате, причина одна: **обучающая выборка слишком мала и не перемешана** — модель видит одни и те же примеры в одном порядке и запоминает их как таблицу.\n\n## Что проверить (по порядку)\n\n1. Перемешан ли датасет между эпохами (shuffle).\n2. Доля дубликатов и near-duplicates в train (быстрый тест — по хешам нормализованного текста).\n3. Соотношение «параметры модели / число примеров»: примеров меньше, чем параметров, — жди запоминания.\n4. Early stopping по валидационному loss включён ли вообще.\n\n## Когда причина другая\n\nЕсли пункты 1–4 чисты, типовые варианты: слишком высокий learning rate (проверить кривую на первых 100 шагах) или утечка train в валидацию.",
    "mediaType": "text/markdown"
  },
  "tag": [
    {
      "type": "Hashtag",
      "name": "nejroseti/obuchenie"
    }
  ],
  "zh:status": "aktualno",
  "zh:version": "3",
  "zh:reviewDue": "2027-01-02"
}